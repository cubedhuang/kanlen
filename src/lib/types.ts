export type WordData = {
	id: string;
	word: string;
	pos: string[];
	meaning: string;
	relatedWords: string[];

	originDefinition: string;
	originLanguage: string;
	originWord: string;
	originTransliteration: string;
};
